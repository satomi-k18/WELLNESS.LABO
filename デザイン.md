# WELLNESS.LABO デザインガイド

このサイトの見た目の基準。新しいページや画像を作るときは、必ずここに合わせる。

## 世界観

**Notion風の「整理された静けさ」×やわらかい自然の配色。**
情報整理のしやすさ・ミニマルさ・文章の読みやすさ（Notionの良さ）を骨格に、
色だけをヨガ・アロマ・心を整える発信に合う、静かで自然な印象に置き換える。
やさしく上品で、女性的すぎず落ち着いた印象を保つ。

やらないこと: 強い黒・強い青・高彩度色・濃い影・過剰な装飾・情報の詰め込み。

## 色

| 役割 | 値 | トークン名 | 使いどころ |
|---|---|---|---|
| 背景（基本） | `#faf6ee` | `cream` | ページ全体 |
| 面 | `#ffffff` | `surface` | カード・記事の面 |
| サブ背景・カード補助 | `#d4e9e2` | `mint` | 帯・画像の下地・補助カード |
| アクセント | `#dfc49d` | `accent` | 飾り・下地・ボーダーの強調 |
| アクセント（文字用） | `#b08d57` | `accent-deep` | ラベル・リンク・hover。accentは淡いので文字はこちら |
| 文字（主） | `#4d4a44` | `ink` | 本文・見出し。やわらかいダークグレー（真っ黒は使わない） |
| 文字（副） | `#8c887e` | `muted` | 日付・補足・キャプション |
| 罫線 | `#e7dfd2` | `line` | 区切り線・枠。太い線は引かない |

## 文字

- 本文・UI: **Noto Sans JP**（400/500）。行間ゆったり `leading-relaxed`〜`leading-loose`
- 見出し: **Zen Old Mincho**（明朝・400〜500）。太字にしすぎない
- 英字ラベル（BLOG / ABOUT 等）: 小さく・字間広く `tracking-[0.2em]`・`accent-deep`

Notionに倣い、**見出しの階層をはっきり**（大見出し→小見出し→本文）。
日本語の見出しは大きくするより、余白で見せる。

## 余白・形（Notionの整理感）

- セクション間: `py-20`〜`py-28`（スマホは `py-16`）
- 本文の幅: 読み物は `max-w-2xl` 前後。読みやすさを最優先し、横に広げすぎない
- 角丸: カード `rounded-2xl`・ボタン `rounded-full`。やや丸みを持たせる
- 影: `shadow-sm` まで。hoverで `shadow-md`。濃い影は使わない
- hover: 小さな変化（色・`-translate-y-1`）を `duration-300`〜`500` でゆっくり
- 罫線: `line` の1px。セクションの区切りは背景色の切り替え（cream ⇄ surface ⇄ mint）で見せる

## 画像

- 明るく、白っぽく、**光がふわっと入っている**こと。暗い写真・強いコントラストは使わない
- 色味は生成り・白・ミント・木。原色と黒は避ける
- 被写体: 植物、光、布、ノートとペン、湯気、窓辺。顔のアップより、手元と空気
- noteのサムネイル（1280×670）もこの基準で作ると、サイトに並べたとき揃う

## 実装のきまり

- 色は `app/globals.css` の `@theme` トークンを使う（`bg-cream` `text-ink` `text-accent-deep` 等）。コンポーネントに直接カラーコードを書かない
- 例外: LINEボタンの緑（`#06C755`）はLINEのブランド色なのでそのまま
